// src/screens/Dashboard/components/cardStyles.ts

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    card: {
        borderRadius: 20,
        marginBottom: 16,
    },
    cardContent: {
        alignItems: 'center',
        paddingVertical: 20,
        gap: 8,
    },
    iconContainer: {
        width: 52,
        height: 52,
        borderRadius: 16,
        alignItems: 'center',
        justifyContent: 'center',
    },
});
